{
  "name": "thewebytes-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TheWebytes DSA Web — Express + MongoDB API server",
  "scripts": {
    "dev": "node --import dotenv/config --watch app.js",
    "start": "node --import dotenv/config app.js",
    "seed": "node --import dotenv/config seeds/seed.js",
    "seed:dbms-meta": "node --import dotenv/config seeds/seedDbmsMeta.js"
  },
  "dependencies": {
    "@clerk/backend": "^1.27.2",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "helmet": "^8.2.0",
    "imagekit": "^5.2.0",
    "mongoose": "^8.14.1",
    "slugify": "^1.6.6"
  }
}
